source 'https://rubygems.org'

gem 'activerecord'
gem 'sinatra'
gem 'sinatra_generator'
gem 'pg'
gem 'bcrypt'
gem 'httparty'
gem 'pry'

# install all the gems in this file by using the 'bundler gem'
# step 1: gem install bundler
# step2: now type 'bundle' in the directory where this gemfile is
# need to run this whenever this file is changed
# need to do this bundle whenever new project
# can run bundle again without problem 